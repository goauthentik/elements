A rather ordinary spinner component.

## Analysis

The Spinner component is just what it says, and spinner exist to tell the user "We're trying to do
something that may take a moment."  It's a simple animation of a single SVG component.  There's not
much more to it than that.

Mapping the size classes to host attributes was therefore straightforward.
